---
name: dynamic-workflow-go
description: Runtime agent composition with programmatic node creation. Go. Use for dynamic workflows that change based on input.
---

# Dynamic Workflow (Go)

Agents and edges created programmatically at runtime. Higher flexibility, less static structure than GraphAgent.

## When to Use

- Workflow shape depends on runtime data
- Agent roles determined by input content
- Dynamic tool loading based on context
